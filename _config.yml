title: DENOG e.V.
baseurl: "" # the subpath of your site, e.g. /blog
markdown: kramdown
env: "production"

exclude: ['_theme', package.json, Gemfile, Gemfile.lock, '.idea']

plugins:
  - jekyll-redirect-from

defaults:
  -
    scope:
      path: ""
    values:
      layout: "page"